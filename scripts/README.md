# Скрипты

*Актуально: март 2026.*

## Шапка и подвал (один экземпляр на весь сайт)

Шапка и подвал хранятся в **одних файлах** и подставляются на все страницы через JS.

- **Фрагменты (редактировать здесь — меняется на всех страницах):**
  - `assets/html/header.html` — навигация (логотип, меню, кнопка «Связаться»)
  - `assets/html/footer.html` — подвал (логотип, ссылки, контакты, копирайт)
- **Подстановка:** скрипт `assets/js/header-footer.js` вставляет шапку и подвал в `#site-header` и `#site-footer`. Ссылки и пути делаются относительными от текущей страницы.
- **После изменения** `header.html` или `footer.html` нужно пересобрать скрипт:
  ```bash
  python3 scripts/build_header_footer_js.py
  ```

На каждой странице должны быть элементы `<div id="site-header"></div>` и `<div id="site-footer"></div>` и подключён скрипт (путь с учётом глубины страницы):

```html
<script src="../assets/js/header-footer.js"></script>
```

(на главной — `assets/js/header-footer.js`, в каталоге на один уровень глубже — `../assets/js/header-footer.js` и т.д.)
